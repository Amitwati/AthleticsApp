<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amitwati.athleticsapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".graph_test" />
        <activity android:name=".ListActivity" />
        <activity android:name=".activity_competition" />
        <activity android:name=".MeasurementActivity" />
        <activity android:name=".AthletesMenu" />
        <activity android:name=".CoachMenu" />
        <activity android:name=".AthletesContestActivity" />
        <activity android:name=".AthletesMeasurmentActivity" />
        <activity android:name=".ShowTrainingsActivity"></activity>
    </application>

</manifest>